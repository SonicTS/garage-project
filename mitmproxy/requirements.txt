# Python dependencies for the mitmproxy addon environment.
# Install in a separate virtualenv if desired:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

paho-mqtt==1.6.1
