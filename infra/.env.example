# Environment example for infra and services.
# Copy to .env and adjust values for your deployment.

# Absolute path to garage-controller config
GARAGE_CONFIG_PATH=/opt/garage-controller/config.yml

# Log level for services (debug|info|warning|error)
LOG_LEVEL=INFO

# Optional: override ports if you wrap services with a launcher that reads env
# POLL_PORT=5002
# TRIGGER_PORT=5001
